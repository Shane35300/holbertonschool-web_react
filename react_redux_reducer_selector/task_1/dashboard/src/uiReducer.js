const initialState = {
	// État initial
  };

  const uiReducer = (state = initialState, action) => {
	switch (action.type) {
	  // Gérer les actions ici
	  default:
		return state;
	}
  };

  export default uiReducer;
